import { FunctionComponent } from "react";
import Component3 from "./Component3";

export type Component92trueType = {
  className?: string;
};

const Component92true: FunctionComponent<Component92trueType> = ({
  className = "",
}) => {
  return (
    <main
      className={`mb-[-19px] h-[1065px] w-[1289px] overflow-y-auto shrink-0 flex flex-col items-center justify-start pt-7 px-80 pb-0 box-border ${className}`}
    >
      <Component3 variant={1} />
    </main>
  );
};

export default Component92true;
